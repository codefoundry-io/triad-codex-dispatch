interface:
  display_name: "Triad Claude Dispatch"
  short_description: "Dispatch a single-shot Claude Code leg"
  default_prompt: "Use $triad-claude-dispatch for an owner-authorized Claude review. State the objective, review target, approved data, exclusions, and result profile before dispatch."

policy:
  allow_implicit_invocation: false
