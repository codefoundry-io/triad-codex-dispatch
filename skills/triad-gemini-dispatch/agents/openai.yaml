interface:
  display_name: "Triad Gemini Dispatch"
  short_description: "Standalone Gemini CLI compatibility consult"
  default_prompt: "Use $triad-gemini-dispatch for one authorized standalone Gemini CLI compatibility consult; it is not a formal Google-family review leg or fallback. State the objective, review target, approved data, exclusions, and result profile before dispatch."

policy:
  allow_implicit_invocation: false
