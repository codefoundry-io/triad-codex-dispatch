interface:
  display_name: "Triad Gemini Dispatch"
  short_description: "Gemini business, Vertex, or API-key fallback"
  default_prompt: "Use $triad-gemini-dispatch only after the agy route is proven unavailable before submission; it is not a direct-request route."

policy:
  allow_implicit_invocation: true
