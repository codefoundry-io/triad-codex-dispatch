# AGY 1.1.10 Formal Route Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use
> `superpowers:test-driven-development` for Tasks 1-3 and
> `superpowers:verification-before-completion` before every completion claim.

**Goal:** Make the 0.2.532 formal Google-family route use AGY 1.1.10's stable
`gemini-3.1-pro-high` selector, fail closed on older or unprobeable AGY, and
finish the pending local formal-review and installation proof.

**Architecture:** Keep the Antigravity wrapper as exact free-string
model/effort passthrough. Put the versioned formal-route policy in the two
review skills, bind it with distribution contract tests, and preserve all
historical round records. Continue the existing full-coverage R46 and host
export workflow only after the changed candidate passes fresh local evidence.

**Tech stack:** Markdown skills and status contracts, Python 3.12/pytest
contract tests, shell-based plugin export/bootstrap checks, native Claude/AGY
provider CLIs, native fresh Codex subagents.

---

## Task 1: Lock the AGY 1.1.10 route in RED tests

**Files:**

- Modify: `tests/test_distribution_contract.py`

1. Change the formal-provider guide expectations from the display-label route
   to the exact `gemini-3.1-pro-high` slug with no `--effort`.
2. Require both formal skills to state the `1.1.10` minimum and a fail-closed
   older/unprobeable result.
3. Require the current status summaries and changelog to state the same route,
   while leaving historical 1.1.7 and old-round evidence legal.
4. Keep the existing generic wrapper passthrough assertions intact.
5. Run the focused contract selection from the workspace root and confirm it
   fails for the old skill/status literals:

```bash
/bin/zsh -lic 'python3 -m pytest /Users/chaniri/codex_workspace/workspace/triad-codex-dispatch-reliability/tests/test_distribution_contract.py -q -k "provider_guides_keep_routes or formal_reviewer_routes or current_route_summaries or agy_110"'
```

## Task 2: Implement the minimal formal-route policy

**Files:**

- Modify: `skills/triad-antigravity-dispatch/SKILL.md`
- Modify: `skills/triad-cross-family-review/references/reviewer-routing.md`
- Modify: `CHANGELOG.md`

1. Require authenticated `agy --version` evidence of `1.1.10` or newer before
   formal dispatch.
2. Use `--model gemini-3.1-pro-high` and omit `--effort` in the exact formal
   argv examples and bounded-route summaries.
3. Treat older/unprobeable versions, missing selectors, rejection, and exposed
   identity conflict as a missing/invalid Google leg.
4. Retain the three-candidate probe recipe only as an AGY-update diagnostic;
   identify the display name as the historical 1.1.7 control, not the current
   production route.
5. Do not change `bin/antigravity_wrapper.py` or add aliases, normalization,
   or automatic fallback.
6. Re-run the focused RED selection and require GREEN.

## Task 3: Align active state and record the live proof

**Files:**

- Modify: `docs/status/2026-07-22-current-state.md`
- Modify: `docs/status/2026-07-22-formal-review-routing-verification.md`
- Modify: `docs/status/2026-07-22-resume-prompt.md`
- Modify: `_runs/releases/0.2.532/resume-after-r45.md`
- Create: `_runs/releases/0.2.532/agy-1.1.10-route-proof.md`

1. Replace only active/current display-label instructions. Preserve old round
   routes and results verbatim as historical evidence.
2. Record the installed version, catalog, vendor changelog fact, three exact
   probe commands/results, and restored Claude marker result.
3. State that the candidate changed after R45 and therefore requires a new
   fingerprint and fresh R46; do not reuse R45 artifacts.
4. Re-run the focused distribution contract and skill validator.

## Task 4: Verify the changed candidate locally

1. Run the wrapper route/preflight tests and the hostile model/effort
   selection.
2. Run a real Antigravity wrapper call with
   `--model gemini-3.1-pro-high` and no `--effort`; require the exact marker and
   successful wrapper/provider exits.
3. Run the full local pytest suite from the workspace root using the recorded
   login-shell Python 3.12 environment.
4. Run distribution/export deterministic checks and `git diff --check`.
5. Record the new HEAD, clean/dirty inventory, canonical diff digest, and
   canonical worktree fingerprint.

## Task 5: Run fresh R46 formal admission

1. Load `triad-cross-family-review` and all required references completely.
2. Prepare a fresh shared directory from the planning base with all repository
   test source included; do not reuse any R45 prepared bytes, prompt, or
   receipt.
3. Record the shared-directory digest and candidate fingerprint.
4. Run all required full-coverage batches for Claude `opus`/`xhigh`, AGY
   `gemini-3.1-pro-high`, and fresh Codex `gpt-5.6-terra`/`xhigh` with no-edit
   and no-execution contracts.
5. Strict-validate every receipt, reproduce findings against the canonical
   worktree, compare post-dispatch digests/fingerprints, and require machine
   admission. Any missing or invalid required leg closes R46 invalid.

## Task 6: Export and prove the installed Codex host

1. Export the admitted source into
   `/Users/chaniri/codex_workspace/workspace/triad-codex-host-sot-refresh`
   using the repository's existing deterministic export path.
2. Run host distribution tests and compare exported bytes with the admitted
   source.
3. Install locally, verify installed/cache byte identity, run bootstrap checks,
   and start a fresh ephemeral Codex session that returns the exact skill
   exposure marker.
4. Record local evidence only. Do not push, tag, publish, create a PR, or alter
   any checkout outside `/Users/chaniri/codex_workspace`.

## Task 7: Resume Argus

1. Update the local continuation handoff with the admitted review ID, installed
   plugin proof, and any remaining risks.
2. Return to `/Users/chaniri/codex_workspace/workspace/Argus-codex`, read its
   nested instructions and current status, and resume from its existing
   governed checkpoint in a fresh session only if context capacity makes that
   necessary.
