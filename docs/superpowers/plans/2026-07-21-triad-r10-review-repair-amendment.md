# R10 functional convergence amendment

## Purpose

R9 was invalid because one Google leg was schema-invalid and two ordinary-use defects remained.
R10 freezes only the compatible repairs and verifies that the normal three-family pipeline works.
It does not reopen rare adversarial filesystem races or add new architecture.

## Accepted repairs

1. Shell-entry preflight now rejects an unterminated shell-RC final line before any persistent
   install mutation, using the same prospective transformation as the writer.
2. English and Korean documentation now state that exact installed launcher paths are
   auto-approved while `on-request` continues to govern other commands.
3. Packet-bound evidence records one real AGY `gemini-3.1-pro-high` call and one fresh Codex call.

## R9 fact checks

- Direct execution with the selected macOS Python returned `MACOS_DIRECTORY_FSYNC_OK`; the two
  Google A directory-`fsync` Critical findings were false and no platform bypass was added.
- Google B was not a valid formal result. Its advisory packet-context claim was contradicted by
  wrapper call sites, local sealed-context validation, the exact R9 control prompt, and passing
  provider-context regressions.

## Verification

- macOS: `588 passed, 6 subtests passed`.
- Pinned Ubuntu 24.04: `587 passed, 1 skipped, 6 subtests passed`.
- Token spikes: `TRIAD_AGY_R10_SPIKE_OK` and `TRIAD_CODEX_R10_SPIKE_OK`.

The R10 verifier reruns the complete deterministic macOS and Ubuntu manifest before packet
creation. Product skill and dispatch-prompt files remain byte-identical to R8; only the generated
one-round review prompts receive the new review identity and a schema-format reminder.

## Review gate

Admit all four legs against identical immutable packet bytes before reading any result:

- two independent exact `Gemini 3.1 Pro (High)` AGY legs, queued sequentially;
- two independent fresh Codex legs with no inherited task history.

Reconcile reproducible evidence rather than votes. A normal-use Critical or Major defect, genuine
nonconvergence, or broken required route invalidates R10. Minor style issues and already deferred
rare adversarial races do not block this functional convergence round.
