# Triad Codex Dispatch restart prompt

Start the new Codex session with saved project root exactly:

`/Users/chaniri/codex_workspace`

Then paste:

```text
Resume the worktree-first Agent-review distribution slice for triad-codex-dispatch.

Development root:
/Users/chaniri/codex_workspace

Product worktree:
/Users/chaniri/codex_workspace/workspace/triad-codex-dispatch-reliability

Read these documents completely, in this exact order:
1. /Users/chaniri/codex_workspace/AGENTS.md
2. /Users/chaniri/codex_workspace/workspace/triad-codex-dispatch-reliability/docs/status/2026-07-22-current-state.md
3. /Users/chaniri/codex_workspace/workspace/triad-codex-dispatch-reliability/docs/status/2026-07-22-resume-prompt.md

First verify the exact worktree, branch, local HEAD, upstream HEAD, and dirty
source/documentation set. Expected branch is codex/triad-reliability-redesign.
The review-base and current upstream HEAD are
80f7a57188ad1a40059be6a2993a1646ef0e76e6. Local HEAD must be either the
review-base hash or its direct child after the one authorized commit. Derive
the exact state from Git and verify that no child commit has been pushed.

Preserve the unrelated dirty checkout at
/Users/chaniri/triad-codex-dispatch. Do not reset, clean, copy over, merge into,
or otherwise modify it.

The owner authorized one combined local commit for the reviewed worktree-first,
Agent-review, routing-policy, and accepted AGY truncation changes. That commit
does not authorize push, merge to another branch, version/changelog changes,
reinstall, release, or pull-request creation. Later Git history or remote-state
mutations go through automatic security review and may present an approval
request.

The normal formal review uses the existing Git worktree. Do not create another
worktree, source packet, copied repository, manifest, allowlist, snapshot, or
Python-generated related-file list. The leader captures one trusted selected Git
status/diff with fixed non-mutating commands and attaches identical output to
all legs. Each no-edit reviewer reads and searches the same --cwd and traces
affected unchanged callers, consumers, tests, schemas, configuration, build
files, and governing docs. Keep provider general shell access denied. Guard the
round with equal pre/post fingerprints covering HEAD, selected diff, untracked
inventory, and untracked content hashes.

Use fresh Codex gpt-5.6-terra/xhigh/fork_turns none, Claude opus/xhigh, and
primary agy gemini-3.1-pro-high. Sol/Fable are conditional long-running
escalations, not routine reviewers. Gemini is fallback-only after proven
pre-submission agy route unavailability.

The distributed human-run bootstrap installs a dedicated profile with
approval_policy=on-request and approvals_reviewer=auto_review. Exact managed
launcher rules use decision=prompt so Agent review evaluates the exact provider
calls. The explicit owner skill invocation supplies one authorization within an
unchanged provider/worktree/scope/data boundary; credentials, authentication
files, environment dumps, provider logs, and unrelated paths stay excluded.
Do not ask the owner again for each matching leg. The explicit
TRIAD_CODEX_PROFILE_APPROVAL_POLICY=never compatibility posture alone uses
decision=allow.

Commit 94a24cb2e59972cd8fccefd06c05a6a7b77166b8 was integrated only at the
functional level: exact own-line bytes/lines truncation markers now produce
terminal `truncated-answer`, quarantine stdout, and bypass schema repair. A
required formal agy leg is invalid and requests a new bounded, compact result;
it does not switch to Gemini after submission. The generic write-file and
sandbox-relaxation workaround, upstream history merge, and release metadata
were rejected.

Current verification evidence is: AGY packet-context runtime 47 passed;
distribution contract 59 passed; archive/read-only compatibility 95 passed plus
6 subtests; all non-bootstrap tests 391 passed plus 6 subtests; and a fresh
Terra/xhigh skill pressure test GREEN. Native codex-cli 0.145.0 evaluation of
current bootstrap-generated rules returned `prompt` for all three default exact
launchers, no match for raw/repository/shell/Python forms, and `allow` only for
the explicit-`never` posture. The final bootstrap run passed all 233 tests. A
preceding run saw one temporary-Python SIGKILL and that exact case passed alone;
do not patch that nondeterministic symptom without a reproducible root cause.

The proposed R15 packet-first dispatch was cancelled before provider execution.
R14 remains historical and does not cover these bytes. No provider was invoked
and no formal verdict exists for this slice.

If local HEAD is still the review-base hash and final review plus verification
are complete, create the one authorized combined local commit. If local HEAD is
already its direct child, do not create another commit. Do not push,
install/reinstall, invoke providers, release, change version/changelog, merge to
another branch, publish, or open a pull request without the applicable separate
owner authorization. Verify the local state and report the exact remaining
boundary.
```
