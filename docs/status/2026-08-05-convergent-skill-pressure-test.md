# Convergent Skill Pressure Test

Date: 2026-08-05

Five fresh-context `gpt-5.6-terra` low-effort samples read the replacement
cross-family skill and received the same pressure scenario used for the RED
control. The requested model and effort were accepted by native spawn; runtime
identity was not exposed.

All five samples selected every required policy outcome:

- remove batching rather than preserve a default, optional, or compatibility mode;
- after a bounded fix, run a fresh complete Claude, Google, and Codex round;
- stop for owner approval before a generalized capability outside approved design;
- require packaged-byte and fresh-process evidence for distribution acceptance.

The initial wording produced only three of five correct batch decisions. Two
samples rationalized compatibility from the old test history. Adding an
explicit unsupported-architecture boundary and distribution acceptance section
produced five of five correct decisions on a fresh rerun.

Machine-readable results are in
`benchmarks/review-policy/focused-convergent-skill.json`.
