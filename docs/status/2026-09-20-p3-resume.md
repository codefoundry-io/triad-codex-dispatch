# TRIAD P3 verification checkpoint

This file preserves the corrected review-input snapshot. For subsequent formal
admission, source commit, distribution and Claude handoff, use the
[current verification record](https://github.com/codefoundry-io/triad-dispatch-spec/blob/codex/host-b-preimplementation-audit/decisions/host-b-p3-verification.md).

## Corrected review-input snapshot — 2026-09-20

The previous restart blocker is resolved: the current task exposes 25 concurrent
agents including the leader, and a fresh spawn succeeded. `close_agent` remains
unavailable; completed children are not treated as running work.

All 243 files recorded by the prior restart snapshot were saved on disk and
matched their recorded hashes/modes before this continuation. The leader then
added real-wrapper regression cases and fixed P3-F1/F2/F3 without changing the
settled contracts or unrelated files. This is disk evidence, not inspection of
an editor's unsaved buffers.

The leader also corrected reserved collector-output input collisions and malformed
request value-type errors. A dedicated fresh RED observed all 12 failures before
the bounded fixes. Related leader verification passed 56 tests.

Current clean-input dedicated GREEN: 27 focused / 1631 full macOS tests,
skill validator and provider-free lifecycle passed. Ubuntu 24.04: 1629 passed
plus 2 existing filesystem skips. Both recorded unchanged source and exact
fixture/container cleanup. Current evidence: `p3-input-refusals-red/`,
`p3-input-refusals-green/`, and `ubuntu-p3-input-refusals/` under the durable
directory below. Earlier F1–F3 evidence remains in `p3-findings-red-r2/`,
`p3-clean-green/`, and `ubuntu-p3-findings-r2/`.

The r2 formal round returned four SAFE results with matching integrity. Its
reviewed bytes were then changed to correct the two input-handling findings,
so a new complete round is required and r2 grants no current admission credit.

Shared case/unit/current-status mappings were reconciled without changing
normative contracts or historical findings. The PRD has a separate current
verification record; unrun B1–B3/A V1–V5 checks remain service evidence gaps.

The next boundary is a fresh complete formal review, followed by reviewed
commit, clean distribution verification and the final published Claude PRD.
The prior failed round contributes no admission credit. Merge, adoption and
installation/release remain distinct operations.

## Historical restart checkpoint

The snapshot below describes the earlier stop, not the current correction or
verification status. Its original findings and evidence are preserved.


Checkpoint: `TRIAD_P3_REVIEW_FINDINGS_RESTART_READY_20260920`.
This is the entry point for the next session. Implementation is incomplete;
passing earlier suites did not cover the reproduced findings below.

## Current state

| Surface | State |
|---|---|
| Development checkout | `/Users/chaniri/codex_workspace/workspace/triad-codex-dispatch-reliability` |
| Branch / HEAD | `codex/agy-web-evidence` / `3d62b7cc370d2c72fc439a431d5f78ab76875b98` |
| Git common repository | `/Users/chaniri/codex_workspace/workspace/triad-codex-dispatch/.git` |
| Remote B main, freshly checked | `56f0f6657084f81516217ee51698f9b18cfc71dc` |
| Remote B feature branch, freshly checked | `3d62b7cc370d2c72fc439a431d5f78ab76875b98` |
| Shared candidate worktree | `/Users/chaniri/codex_workspace/_runs/infra/20260919-host-parity-second-pass/spec-agreement-amendment` |
| Shared candidate | `codex/host-b-preimplementation-audit`, `aef3adee863fd90fdfab60ae2253717cf1b4d303`, clean |
| Shared remote main, freshly fetched | `2eb883fee59e66556ee7c7f87189b38231136622` |
| Claude host A, read-only | `/Users/chaniri/triad`, `92c8afd500499d8736afcc28b39a87a4f87fed50`; tracked tree clean |
| Current local P3 source | Intentional uncommitted implementation, tests and docs; preserve all bytes |
| Current admission | Failed round; no P3 formal admission, commit, push, merge, install or release |

The common B checkout has a different local main and intentional unrelated
changes. Do not reset, clean, stash, discard, rebase or create a separating
checkout. Read live state before any new action; this table is not a substitute
for the next session's checks. Existing PRs are B
[37](https://github.com/codefoundry-io/triad-codex-dispatch/pull/37) and shared
[1](https://github.com/codefoundry-io/triad-dispatch-spec/pull/1).

## Required corrections

| ID | Confirmed issue | Next bounded action |
|---|---|---|
| P3-F1 | `bin/review_round_v2.py:341-343` requires complete stdin for every CLI. Healthy AGY/Gemini argv delivery reports `not-used` and is refused. | Add real Google wrapper-run-log ingestion regressions, preserve Claude stdin refusal controls, then make delivery validation route-specific. |
| P3-F2 | Schema-valid omitted adapter subkeys cause uncaught lookup exceptions without preparation-failure custody. Reproduced for a new Claude entry without `agent` and native omitted `model`; related selected-route and empty-selection branches also need tests. | Preserve documented default semantics or issue a controlled capability refusal, recording the failed preparation. No extra schema or blanket fallback. |
| P3-F3 | v2 Gemini copies its preflight version into transport, while documentation separates preflight from an unexposed runtime version. | Prefer keeping unexposed v2 runtime version null with preflight evidence preserved; retain legacy behavior and test the real wrapper boundary. |

These are bounded implementation corrections under settled contracts, not an
established defect in the shared design. The leader diagnosed them without
changing production source. Mandatory dedicated RED has not run for these fixes.

## Evidence and review state

Earlier canonical fresh Terra/high verification: 24 focused tests, 1604 full
macOS tests, skill validation and provider-free lifecycle passed with unchanged
source. Independent Ubuntu 24.04: 1602 passed, two existing filesystem skips.
Those results are retained, but the newly reproduced cases were absent.

Round `triad-b-transport-p3-r1` reviewed digest
`b6ea67bc2b32e74b25dc22ca4256b3d4f5b5a471e0b2525de0d41fcfbd9e036e`.
Claude Opus/xhigh returned NOT-SAFE; AGY Pro/high and Flash/high returned SAFE.
Fresh native Codex failed to start with `agent thread limit reached`; no handle
was allocated. Every started CLI terminated and its result was captured.
Final integrity matched, evidence was exported, and only recorded disposable
staging/custody was cleaned. The ledger remains FAILED. No running review
process remains to poll, and no result carries credit into the next round.

The workspace `agents.max_threads` was changed from 12 to 24 at the owner's
request. CLI configuration loading succeeded; the active task still rejected
fresh spawns. The current native tool surface has no `close_agent`. If a new
session exposes the supported close operation, consume results and close only
completed children whose work is finished. Do not substitute interruption,
archiving, deletion, old child context or a Codex subprocess for a fresh child.

OpenAI feedback was successfully submitted through the documented
`feedback/upload` API, with `includeLogs=false` and no attachments. Returned
feedback conversation ID: `01a0a938-0ca4-7fd0-bd38-d2c8bd31e75d`.
This is a successful submission receipt, not a support ticket or resolution.

## Read and resume in this order

1. Read applicable workspace/project instructions and the internal
   [completion checklist](../superpowers/plans/2026-09-20-host-parity-completion-checklist.md).
   Verify Git/common-root/dirty state and fresh native capability; preserve all
   existing changes. Fetch shared main before common decisions and final review.
2. Read `p3-review-adjudication.md`, `p3-review-findings-reproduction.json` and
   `p3-implementation-notes.md` in the evidence directory below. The first two
   distinguish executed reproductions from static observations.
3. Write bounded tests against the canonical source. Run fresh exact
   `triad-skill-executor`, Terra/high, `fork_turns=none`, for intended RED before
   production edits. The root leader applies the smallest fixes. A separate
   fresh executor performs GREEN, full relevant tests, validator and lifecycle.
4. Verify both supported platforms and run a new complete required formal round
   on identical current bytes. Use the current workspace composition; the prior
   exception required Claude Opus/xhigh, Google Pro/high, Google Flash/high and
   fresh Codex Terra/xhigh. Capture every terminal result and final integrity;
   export before exact cleanup. Do not rerun old exclusive cleanup helpers.
5. Reconcile every C1-C30 case plus rules, contracts, prompts and units. Shared
   `units.json` and the old handoff still contain stale P3/D-5 status and must
   be updated against the final verified implementation. Authentication-policy
   manifests B1-B3/A V1-V5 remain NOT RUN unless actually executed. AGY URL-body
   prefix loss remains the owner's nonfatal known issue, not a vendor-fix claim.
6. Complete internal obligations, required review, reviewed commit and clean
   distribution verification. Respect current merge/adoption/release boundaries.
7. Finish the Claude PRD: functions, interfaces, constraints, exceptional
   behavior, migration and acceptance criteria, with exact B/A source references.
   Remove checklist/progress/failure history from the delivered PRD; retain it
   internally. Publish shared updates on one identified basis for Claude. Do
   not edit A or present a candidate as installed/adopted.

## Durable local evidence directory

`/Users/chaniri/codex_workspace/_runs/infra/20260920-transport-contract/`

- `P3-RESUME.md`: detailed restart context and remaining work.
- `p3-review-adjudication.md`, `p3-review-findings-reproduction.json`: accepted findings.
- `p3-final-green/`, `ubuntu-p3-final/`: earlier actual verification receipts.
- `p3-source-consumer-map.md`, `p3-case-evidence-map.json`: internal coverage index.
- `claude-implementation-prd-draft.md`: unfinished delivery draft, not published.
- `openai-agent-lifecycle-feedback.md`, `openai-feedback-request.json`,
  `openai-feedback-response.json`: submitted report and receipt.
- `restart-state-20260920.json`: live Git state, source-file hashes and evidence checks.
- `NEW-SESSION-PROMPT.md`: concise next-session instruction.

The failed review's original evidence is separately retained at
`/Users/chaniri/codex_workspace/_runs/reviews/triad-b-transport-p3-r1/`.
These durable task records are not ordinary auto-pruned failure IPC.
